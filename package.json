{
  "name": "city-wallet",
  "private": true,
  "workspaces": [
    "app",
    "dashboard"
  ],
  "scripts": {
    "app": "npm run start --workspace=app",
    "dashboard": "npm run dev --workspace=dashboard",
    "api": "cd api && uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  }
}
